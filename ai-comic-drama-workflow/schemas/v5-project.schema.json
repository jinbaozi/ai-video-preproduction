{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "v5-project",
  "type": "object",
  "required": [
    "schema_version",
    "workflow_id",
    "project_id",
    "execution_mode",
    "delivery",
    "mode",
    "max_shots_per_task"
  ],
  "properties": {
    "schema_version": {
      "const": "5.0"
    },
    "workflow_id": {
      "const": "ai-comic-drama-v5"
    },
    "project_id": {
      "type": "string",
      "minLength": 1
    },
    "execution_mode": {
      "enum": ["current-agent", "codex-agents"]
    },
    "orchestration_protocol": {
      "const": "6.0"
    },
    "delivery": {
      "enum": [
        "full",
        "text-only"
      ]
    },
    "mode": {
      "enum": [
        "text",
        "reference",
        "keyframe",
        "edit",
        "extend"
      ]
    },
    "max_shots_per_task": {
      "const": 5
    },
    "production_target": {
      "enum": [
        "none",
        "video"
      ],
      "description": "none keeps the preproduction endpoint. video adds a separate VIDEO_DELIVERED endpoint. Omitted values mean none."
    }
  },
  "allOf": [
    {
      "if": {"properties": {"execution_mode": {"const": "codex-agents"}}},
      "then": {"required": ["orchestration_protocol"]}
    },
    {
      "if": {"required": ["orchestration_protocol"]},
      "then": {"properties": {"execution_mode": {"const": "codex-agents"}}}
    }
  ],
  "additionalProperties": true
}
