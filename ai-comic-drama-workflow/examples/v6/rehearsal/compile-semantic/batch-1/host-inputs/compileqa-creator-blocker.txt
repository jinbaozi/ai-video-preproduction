Confirmed root cause: locked V5 adapter `src/ai_comic_drama_workflow/v5_adapters.py:195-203` copies shot composition subjects' `visible_parts` into both `start_state` and `end_state`; source StoryboardIR `state_end.PROP_LETTER.pose` says letter inside backpack, timeline state at 10500/12000 has `visible_parts=[]`, and AVIR prompt correctly says `可见部位：无` at 10.5 s. Locked VPC contract explicitly says out-of-frame end states use `visible_parts=[]`. I will mark compile_review semantic equivalence blocked with owner compiler/adapter unless corrected through a new compile version; this is a real detected contradiction.
