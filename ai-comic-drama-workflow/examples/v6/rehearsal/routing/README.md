# Static target decision rehearsal

The original text-only demo reached a V5 `target` decision after the Control package was accepted at V6 revision 293. The root agent explicitly authorized `seedance2.0` only as an existing `prompt_plan` profile for static text compilation in this original rehearsal. The locked profile itself states that its transport is unresolved. `target-routing-evidence.json` binds the decision ID, pre-decision V6 revision/state hash, locked module and registry SHA-256, profile digest, scope and authorization text. `target-routing-decision.json` is the exact native decision submission; `resume-output.json` shows `project.target=seedance2.0` was saved without an external call.

The immediate V6 `run` then returned `FILE_HASH` because a prior STALE visual_media skip still expected the pre-decision `project.json` bytes; see `run-error-output.json`. The gateway was corrected to archive the exact earlier project bytes as content-addressed historical evidence. A retry reached the pending Compile specialist at revision 324; `run-recovered-output.json` is the recovered action. This archive does not claim the compile has passed review. No media or model API was called.
