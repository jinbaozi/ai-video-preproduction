# Storyboard semantic rebinding rehearsal

The prior accepted StoryboardIR became STALE after V5 snapshot URI relocation changed its hash-bound semantic review. This is a fresh Storyboard task on current Canon, Director and Art. A real creator and a distinct real reviewer used Codex dispatch receipts, frozen inputs, and locked storyboard-grammar resources. Creator candidate and independent review passed three declared checks. Fresh locked native status is `VALID` (matching RoleResult), package verify is `VERIFIED`, the semantic digest binds the submitted IR, and 1437 V5 plus three V6 handoffs were checked. Kernel accepted this Storyboard at revision 276 and advanced to Control at revision 279. Camera/light statements in the IR are static intentions; real media, 3D projection and collision QA remain `NOT_RUN`.

`candidate/` is a selected archive: it includes original IR, full RoleResult, candidate and evidence, but omits the large duplicate IR and detailed coverage files from the live native package. The complete package stays in the temp project. `review/` holds independent review evidence. Actual host spawn responses, dispatch receipts and structured message wrappers are here. Check `SHA256SUMS`. Absolute temp paths in historical records identify the live project, not a standalone install.
