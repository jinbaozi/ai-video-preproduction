坐标核对已修正我的初步疑虑：这是两套显式基准，不是冲突。Director `/scenes/0/coordinate_system` 的静态帧为 X-right,Y-depth,Z-up，`/shots/0/camera/start_m`=[0,-2.8,1.2]；Director `/timeline/coordinate_system` 为 x-right,y-up,z-depth，`camera_operations/start_position` 明写“时间轴坐标”[0,1.2,-2.8]，两者经过 `[x,y,z] -> [x,z,y]` 对应同一机位。仓库 `v5_handoff.py:51-55` 也明确定义静态字段变换而 timeline 1.2 不变。ArtIR.set 与其编译 S1 prompt 第7行只使用静态场景基准，未重写相机数值。先前称会传错坐标不成立，我继续审阅，当前倾向 handoff PASS。
