# Art rebuilding rehearsal

After the earlier ArtIR became STALE, this fresh scene batch was dispatched to a real Codex creator and an independent reviewer. The creator read frozen inputs and the locked production-design-grammar Skill, produced `candidate/`, and ran a fresh native validator (`STATIC_VALID`). The reviewer independently checked the three V6 checks, six V5 hard handoffs, two V6 source mappings and the Canon promotion URI relationship; all review checks were PASS. The kernel accepted Art at revision 238. Offline compile was `PLANNED`; no image/video submission or media QA occurred (`NOT_RUN`).

The subsequent scheduler attempt returned `COMMAND_COLLISION` while trying to recreate an old STALE visual_prompts skip task. `art-rebuild-review-submit-output.json` records that error. The accepted Art bytes and state were retained. The scheduler was corrected to bind skip IDs to the current accepted predecessor and later advanced to a new Storyboard task at revision 261; this Art archive does not claim downstream completion. Actual host spawn responses, dispatch receipts, message wrappers, candidate and independent review are copied here. Check `SHA256SUMS`. Absolute temp project paths in historical receipts identify the live rehearsal and are not standalone installation paths.
