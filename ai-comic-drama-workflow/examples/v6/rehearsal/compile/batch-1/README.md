# Compile batch 1: genuine independent FAIL

A real Codex compiler specialist created a strict V6 candidate from 27 frozen inputs and five locked resources. Four candidate checks and locked native AVIR/compile-manifest verification passed; the static package was labelled `COMPILED`. A distinct real reviewer found a semantic camera-coordinate contradiction not detected by those validators. `frozen-build/prompt.txt` presents camera-operation position `[0,-2.8,1.2]` alongside numeric camera position `[0,1.2,-2.8]` for the same locked shot. DirectorIR first supplied the inconsistent vertical coordinate despite declaring y-up/chest-height; StoryboardIR corrected the numeric position but retained the old operation text. Locked adapter and compiler propagated both.

The reviewer recorded `compile_manifest=FAIL`, `failure_owner=director`; other declared checks PASS. The V6 kernel rejected the candidate at revision 339 and returned `RETRY_REQUIRED`. No CompilePackage or AVIR was accepted. The source package also retained segment `DRAFT_REQUIRES_TARGET_CHECK`, prompt `PENDING_AGENT_REVIEW`, post obligations `PLANNED`, transport unresolved, media `NOT_RUN`, submitted/runnable false. The old bytes and decision evidence remain here; subsequent owner repair must create new versions. `SHA256SUMS` checks the selected archive, while the live temp project retains all frozen build files.
