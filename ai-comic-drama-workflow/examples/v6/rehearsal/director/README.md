# Director 真实宿主演练

运行项目：`/private/tmp/ai-video-v6-real-efKlQy/project`。任务 ID：`TASK_b1e7ff7ce83d7a32bfa09935`。原始短句、Canon 与 Screenplay 证据见相邻目录。锁定的 `director-grammar` 模块位于运行项目，本归档只保存必要的候选与宿主证据。

`batch-1/` 包含实际 Codex `collaboration.spawn_agent` 返回、按原始返回登记的派发回执、结构化消息、创作者候选、原生检查，以及独立审阅记录。创作者 `/root/host_bridge/v6_06e72da81da7989f0b415f37` 的锁定模块检查与本地原生校验通过；独立审阅者 `/root/host_bridge/v6_1cac274d3e21ae688426fb78` 发现 `handoff` 实质性失败：信已由乙收入背包，而镜头终态与后续采样仍说甲握着同一封信、乙尚待接信。`batch-1/review/handoff.review.json` 指明矛盾字段，`review-record.json` 保留逐项结论。内核收到 FAIL 后未接受候选，自动创建第 2 批。

`batch-2/` 保存内核自动生成的新冻结批次、实际派发回执、带内容哈希的返修交接、候选和独立审阅。第二批修好了旧的终态矛盾，但审阅者又发现起点人物姿态写“右手持信”，人物 `contact` 却写“无接触”，与道具接触 `CHAR_JIA.right_hand` 冲突；后续接触采样也不一致。原生静态校验仍通过，独立 `handoff` 再次判 FAIL。内核在修订 64 返回 `REPAIR_REQUIRED`，未接受候选或继续自动派发。

各批原始候选均保持原字节；每批的 `SHA256SUMS` 记录归档文件哈希，`frozen-record.json` 保存该批输入、执行身份、候选与审阅记录。此演练不证明图片或视频生成质量。

`batch-3/` 保存内核依据两次真实失败事件冻结的 `repair-brief/6.0`、失败文件、第 3 批任务信封、真实派发回执、消息、候选和独立审阅。创作者在用量中断后通过同一已登记智能体接续，未重派；四个旧缺陷逐项修复。审阅者独立复核六个时点与 31 次动作变更，三项检查均为 PASS。首次审阅记录把候选文件 SHA 误写为协议候选摘要；内核预检拦截后，同一审阅者保留 `review-record.v1.json` 并修正绑定，内容判断未变。内核在修订 89 接受第 3 批并建立下一分场美术任务。`batch-3/SHA256SUMS` 可核对归档字节。

`rebuild-1/` 记录后续控制包诊断发现 V5 快照重定位使已接受 DirectorIR 的语义审阅绑定失效之后，内核创建的新 Director 责任批次。新的创作者和独立审阅者分别用真实 Codex 工具派发，逐项核对六条 V5 与两条 V6 交接；内核在修订 220 接受新候选，随后创建新分场美术任务。旧批次的成功与失效记录仍保留，未改写为新的历史。

`upstream-repair-1/` 记录 Compile 批次 1 独审发现相机坐标表述矛盾后，内核按 `failure_owner=director` 冻结失败审阅并创建的新 Director 任务。实际创作者和独立审阅者分别核对锁定的场景与时间轴坐标约定，确认两个数组通过轴序换算表示同一固定机位，并在机位文字中明确时间轴坐标。新候选在修订 386 获独立审阅和内核接受；旧 Compile 失败历史仍保留，下游产物须重建。归档含两次真实派发、候选、检查与审阅的 43 份文件和 SHA-256 清单。
