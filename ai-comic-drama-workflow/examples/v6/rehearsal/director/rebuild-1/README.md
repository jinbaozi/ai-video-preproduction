# Director semantic rebinding rehearsal

Real Codex creator and separate reviewer executed this fresh Director batch after the earlier accepted DirectorIR became STALE due to V5 snapshot URI and semantic-review rebinding. The source text, frozen task, native Skill resources and six V5 plus two V6 handoff requirements were read from the live project. The creator produced `candidate/`; the independent reviewer produced `review/` with three PASS findings and a fresh locked native check. `event-excerpt.json` captures the accepted V6 state and published artifact index after `agent-review` at revision 220 (the project then advanced to Art at revision 223).

The raw `spawn-director-rebuild*.json` responses, dispatch receipts and structured message wrappers are actual host evidence; no media was generated or reviewed (`media_review=NOT_RUN`). Earlier stale Director/Storyboard failures and cancelled Control are preserved in their existing rehearsal folders. File references under `/private/tmp/ai-video-v6-real-efKlQy/project` identify the live project at rehearsal time; this copy is an evidence archive, not a standalone migrated project. Check `SHA256SUMS` for copied bytes.
