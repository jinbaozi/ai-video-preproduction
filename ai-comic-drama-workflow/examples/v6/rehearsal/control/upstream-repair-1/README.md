# Control 跨责任下游重建

源项目：`/private/tmp/ai-video-v6-real-efKlQy/project`。当前 Director、Art、Storyboard 重新审结后，任务 `TASK_ddd2a77146e2ddd7866420cb` 经真实创作者与独立审阅，于修订 479 获 V6 `ACCEPTED`。本目录保留实际 Codex 派发返回、正式回执、候选、交接、审阅和部分控制包文件；完整大体积帧预览与适配诊断留在运行项目。

锁定 StoryboardIR→AVIR 适配为 `MAPPED`、零损失，控制包 build `DERIVED`、verify `VERIFIED`；独审重建的 11 个文件与候选逐字节一致。121 帧摄影机为 `PLANNED_PROJECTION`，14 项关键帧请求为 `DRAFT_REQUIRES_HOST_REVIEW`。环境根点 `N_ENV` 仍未确定，实际媒体、模型轨迹与碰撞检查仍为 `NOT_RUN`。`SHA256SUMS` 核对本目录字节；这些静态规划状态不证明生成模型已执行或遵循控制。
