# Control package rebuild rehearsal

After the earlier Control task was cancelled for stale semantic provenance, this new task used the accepted rebuilt StoryboardIR. A real Codex creator and a separate independent reviewer read frozen inputs and locked video-prompt-compiler resources. The creator built a ShotControlPack; locked AVIR validation was `VALID`, adapter `MAPPED` with zero loss, original and independent rebuilt packages both `VERIFIED` with 11 matching files, and V6 pre-review passed. Independent module, control_verify and handoff checks were PASS. Kernel accepted the package at revision 293.

This is **static planning evidence**. 121 camera frames have `PLANNED_PROJECTION`, not observed model motion. Fourteen keyframe requests remain `DRAFT_REQUIRES_HOST_REVIEW`; the scene environment root lacks numeric coordinates and its point status remains `UNDETERMINED`. `controls=[]`, 50° FOV is an `authored_proxy`, and actual model control, movement, contact, collisions and media review are `NOT_RUN`. The readable top-down blocking SVG is `candidate/control-pack/review/blocking-001.svg`.

This selected archive keeps the original candidate, manifest, config, AVIR, review summaries and host receipts. It omits the full 121-frame JSON, large adapter/source dumps and review HTML; the live temp project retains them for exact locked verify. Check `SHA256SUMS` for copied bytes. A later routing decision selected an existing `seedance2.0` prompt-plan profile for this rehearsal's static text compile only; its transport remains unresolved and no paid generation was attempted. The V6 run after the decision exposed a stale skip evidence hash issue, recorded separately under `../../routing/`.
