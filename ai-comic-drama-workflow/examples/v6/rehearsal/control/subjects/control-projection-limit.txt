Same control batch now builds and locked verify succeeds: AVIR MAPPED/losses=0, `vpc.py validate` VALID, `vpc.py control build` DERIVED, `control_cli verify` VERIFIED/NOT_RUN. However build reports `projection_unresolved_frames=121/121`: accepted StoryboardIR has static camera start/end position and locked operation, but no N_CAMERA position motion track, and locked renderer requires camera node position. Top-down root trajectories/state are present; perspective frame projection/keyframe readiness remains UNDETERMINED. I asked adapter owner whether to derive static camera track from locked source fields. Will not overstate control efficacy.
