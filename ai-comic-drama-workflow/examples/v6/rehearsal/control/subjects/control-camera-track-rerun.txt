I now see `_locked_camera_tracks` landed in shared `v52_adapters.py`; it derives world/m hold N_CAMERA only when source shot start=end, single locked operation covers entire shot, no overlapping native camera position track, and records source→target coverage. I am rerunning the same batch1 candidate against this current code and will require `projection_unresolved_frames=0` before formal RESULT.
