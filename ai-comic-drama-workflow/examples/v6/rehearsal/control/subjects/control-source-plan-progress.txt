While adapter/native gate repairs run, I froze input/resource SHA checks (`all_match=true`) and an auditable source-only top-down plan at `candidate/evidence/topdown-source-plan.json` plus `topdown-review.svg`. These preserve corrected action ID, root nodes, fixed camera, letter trajectory, six state samples, action windows, panels, and explicit limits. `control-config.json` is schema-shaped, but locked compiler cannot build/verify until adapter fix. Exact failing command outputs are in `evidence/native-av-ir.json` and `evidence/control-build.json`.
