Important control QA: locked `review/blocking-001.svg` currently shows JIA/YI/LETTER/BACKPACK root trajectories but omits camera node entirely; all 121 sampled frames have `N_CAMERA.position=null` and `camera.status=UNDETERMINED` despite static shot camera coordinates. This is a real execution limitation for '镜头轨迹控制' and keyframe planning. I suggest wait for deterministic static camera track derivation from StoryboardIR `shots[0].camera.start_m=end_m` + locked CAM operation, or accept with explicit unresolved limitation only if root chooses. I asked adapter owner; no formal RESULT yet.
