Re-audit found a real blocker, so I will retain unresolved. The locked ArtIR schema permits events=[] (minItems 0), but art_compile.states() consequently carries PROP_BACKPACK.initial_state.condition='待收纳同一封信' unchanged through S_STOW; frozen Director /timeline/state_samples/5-6 says the bag has already received the letter at 8500/9000 ms. Shot prose records the ending, but the native Art state-after contradicts it. Since locked art_compile.validate requires any Art event to cite /shots/N/phases/K and all three Director phases arrays are empty, I cannot encode that transition without a Director-owned phase repair. I am recording this exact audit and downgrading the handoff check while preserving the native STATIC_VALID result.
