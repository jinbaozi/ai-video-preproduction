I confirmed the batch-2 failure path in native V5: `validate_handoff` expects six screenplay mapping rows in RoleResult.handoff, each carrying an identical `review` stamp. Batch-2 supplied two V6 artifact bindings there. I will preserve DirectorIR bytes, rebuild only batch-3 candidate metadata/evidence with six native rows, and keep two graph bindings in candidate-result.handoffs.
