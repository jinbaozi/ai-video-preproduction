I confirmed the submitted SHA-256 72b516…bcca has a material final-shot continuity defect. At 9000 ms the letter state is supported by PROP_BACKPACK.interior, but COMP_S_STOW still frames N_PROP_LETTER as visible, foreground, unobstructed through 9000 ms; panel_state(9000) returns both. Native dg.py validation still reports STATIC_VALID, so this is an independent semantic failure under native_validator. I’m writing a V6 review record with failure_owner=director; hash/module and handoff binding checks otherwise pass.