Found a locked-contract contradiction before editing: director-grammar/schemas/director-ir-1.2.schema.json defines /shots/items/phases as {"const": []} (line 565); 1.1 does too. The Art compiler requires event.director_pointer under /shots/N/phases/K. Therefore a nonempty phase list makes the current 1.2 DirectorIR invalid, while an empty list makes Art events impossible. I am checking whether the native 1.0 schema can preserve all current 1.2 timeline semantics; otherwise this needs a module-owner contract revision, not just a Director candidate.
