当前 full image-prompt-optimizer 1.15.7 需由锁定 V5.accept_prompt 原生入口在独审前复核。请在 RoleResult 真实填写 understanding、prompt、params、quality_check、逐个锁定 ref 的 checks；`production_contract` 必须指向本批目录内实际存在且能通过锁定 `scripts/validate_production_contract.py` 的文件，并在候选证据保留其 SHA-256。请按本资产的 Art 锁定项独立写检查结论，避免与另一角色资产完全相同的 checks。图片尚未生成，media/视觉审核如实标为 NOT_RUN；不要调用任何付费图片/视频 API，也不要派辅助智能体。只在当前冻结 batch 1 目录提交 VisualImagePrompt、V5 RoleResult、V6 candidate-result 和真实交接证据。
