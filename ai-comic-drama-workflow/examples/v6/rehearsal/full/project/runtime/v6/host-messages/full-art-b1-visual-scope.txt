The full-delivery graph derives visual_prompt jobs from accepted ArtIR assets that carry a Canon entity_id. Please include the visual asset cards genuinely needed for this one scene and bind each to an existing Canon entity ID; preserve unspecified identity, location and prop details as design proposals rather than source facts. Keep image reference plans as text until media is actually registered. Do not suppress needed assets merely to skip the image-prompt stage. Keep the native V5 handoff rows distinct from the two V6 candidate handoffs, and copy the fresh locked validator status exactly.
