# 阶段7

文字分镜：保留源镜头顺序。为各片段建立镜头ID、时长、人物动作和情绪、相机与声音轨道、初末状态和连续性原因。每镜默认1个故事板关键点，交接或显著变化选2—3个。身份资产ID可预先稳定分配；此阶段不生成图片。每批最多5镜，接续状态不能遗漏。

严格使用当前TaskEnvelope内的output_schema，只加载当前列出的上游资料。source_refs引用来源ID或上游正式产物路径。低风险补充写入inferences；自动QA不可冒充用户批准。
