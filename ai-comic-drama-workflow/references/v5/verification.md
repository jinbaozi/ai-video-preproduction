# 历史 V5 验证范围与当前增量

本次实现使用固定咖啡馆原文、两名成年角色、一个场景、三镜头进行交接测试。源码测试与交付状态分开记录。

- V5专项回归35项通过：原生协议、所有权交接、独立导入、任务指纹、重复提交、分批继续、图片哈希/绑定/身份版本、局部失效、明确决定、恢复、模块锁、旧项目复制和提示词复用。
- 五个专业技能适用测试分别为：导演29、美术52、分镜86、视频编译24、图片尺寸校验6，已在源码环境通过。
- 三镜头静态示例从项目事实到视频提示词完成，保留对白、身份、信封持有、坐标与时序；输出是text-only，实际视频与媒体效果NOT_RUN。
- 总包单独安装、逐包隔离解压、适用测试、quick_validate、同源内置包校验与逐包重复构建由`scripts/verify_suite.py`验证；实际发行记录在工作区outputs/v5-implementation/release-verification/report.json，记录对应的六个归档哈希。
- V2 到 V4 运行路径已从本包删除。旧项目复制仍由 V5 回归覆盖，不依赖已删除的 V4 内核。历史基线日志在工作区 outputs/v5-implementation。

## 真实图片

宿主image_gen实际返回了一张1672×941咖啡馆参考图，已查看并核对两位成年虚构角色的发型、服装、桌面、窗光与蓝色信封。它是身份候选，不能代替精确画格、手别或完整动作时序验收。

当前记录为REVIEWED_IDENTITY_CANDIDATE，身份首次定稿仍需用户选择；完整三镜头实际图片验收为PENDING。图片原件、实际哈希、提示词、尺寸与查看结论在工作区outputs/v5-implementation/real-media/review.json。没有把单元测试合成PNG当作真实验收，没有填写虚构用户批准。

本套装不生成实际视频、不配音、不剪辑、不发布。专业包原有submitted=false、NOT_RUN回执保持不变；本次真实图片另有项目绑定与审核记录。静态映射及自然语言合同检查仍需要当前Agent的语义审查，不能由Python断言证明未来模型的视觉效果。


## V5.1 新增验证

新版时间轨和完整内容语义指纹的检查记录在工作区 outputs/v51-implementation。三镜样例走 DirectorIR 1.1、ArtIR 1.0、StoryboardIR 1.1、AVIR 1.1；原生及旧版代表任务都须在隔离解包环境验证。发行脚本从各 SKILL metadata 读取版本，不再写死。

真实参考视频只记录实际查看的前 3 秒采样及 2–2.5 秒加密观察；原始视频约 30 秒，其余未查看范围不标已验证。听审未执行，真实图像的 V5 身份候选证据不升级为新的批准；本次没有生成视频或声轨。

完整制作仍由当前 Agent 逐项复核。覆盖表与结构测试不证明未来生成的画面、口型或摄影机轨迹符合设计。

## V5.2 验证

新增原生运动/空间和工作流依赖回归；发行检查同时执行 1.0、1.1、1.2 代表任务，核对独立包与内置模块完全一致及重复构建哈希。工作区 `outputs/v52-implementation` 保存本轮静态结果。数值关系检查点、语义复核、真实图像和参考视频范围、视频执行分别报告；缺失的真实媒体验收不由测试替代。

## V5.3 编剧接入验证

本轮的编剧、导演交接、总工作流与隔离发布证据在工作区 outputs/screenplay-implementation。历史段落中的测试数与真实图片记录仅适用于各自旧版本，不作为新增编剧功能或本轮媒体结果的证明。当前七包隔离报告由 verify_suite.py 生成，包含实际命令、退出码、包哈希、同源内置包与重复构建结果。
