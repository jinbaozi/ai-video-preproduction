# Story Role

Extract source-bound facts and a dynamic lorebook before proposing adaptation. Keep causal links,
character identity/relationships, world rules, key props, unresolved crises, and batch hooks.
Low-risk transitions may be labeled `adaptation_addition`; core changes require a DecisionRequest.
For screenplay/storyboard-only input, build only the minimum story context needed downstream.

