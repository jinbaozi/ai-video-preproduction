# 验证记录

日期：2026-09-19；版本1.0.0。使用Python 3.12与jsonschema 4.26.0完成本地验证。

- Skill标准frontmatter/命名/未完成脚手架检查通过。
- 24项行为回归通过：schema、跨模型条款、台词口型、音轨路径、空间终态、连续性、轴侧、微表情可见性、
  时轴、来源与哈希、参考角色/槽位/上限、Agnes模式与参数、未知能力、预算、Context纯函数与fallback、
  扩写决策/trace、构建完整性及批量错误隔离。
- 独立前向测试发现后期对白口型依据缺失、终态相对位置未验证，两处已修复并复核通过。
- 独立目录按SKILL命令完成profiles→validate→compile→verify→replay。
- 最终发布包执行独立解包检查、内容哈希/安全路径核对、Schema自检、链接检查及CLI烟测；详见交付目录`outputs/verification.json`。

示例为合成资料，测试参考字节只验证映射逻辑，不是经过视觉QA的真实图像。
Seedance2.0/2.5、Agnes2.5/Flash、Kling3/Omni、H3、Veo3.1有可运行的提示词编译路径。
Agnes可形成文档约束下的API草案；其余是提示词计划。Wan3.0和RunwayGen4.5因具体能力未核实而明确BLOCKED。
无上传/付费/生成/API实测，没有VBench、真人A/B、口型或成片质量通过的声明。
报告中的企业服务、模型训练、异步生成与SLA作为宿主扩展路径，未作为已实现功能交付。

## 2026-09-24 · 1.4.0 按镜头正文投影

- 在仅纳入本项修改的隔离工作树中，编译器原 96 项回归通过；新增 Seedance/Kling/Agnes 布局断言所在的 7 项投影测试通过。V5.2 工作流 9 项、V5 导入与续作 35 项通过；七个 Skill 的格式校验和 V5.2 共享文件同步校验通过。
- 咖啡馆 AVIR 1.2 夹具可生成 `compiled-artifact/1.3`，正文镜头与时间顺序、逐项动作/运镜、后期对白口型和片段覆盖断言通过；删除正文片段会触发覆盖校验。旧 AVIR 1.0/1.1 保留原分支。
- 海边街拍 60 秒输入的内容指纹为 `327106e02a489186c7518289a58055adb3eb5360151471858957e6b22b852b56`；Seedance 2.0 正文指纹为 `c1f55cc99e8c7e733e7a6bd49c4200bdb0c4d29787d641b1eb998a05017338e6`。完整正文 40160 字符、五个镜头；静态状态只在首次可见和变化时表述。编译状态仍为 `BLOCKED`（`E_TARGET_DURATION`），`W_TRANSPORT_UNRESOLVED` 仍提示渠道待核验；包 `verify` 通过，未提交或生成视频。
- Agent 对原 AVIR 和上述正文的绑定复核发现：S5 的 `WOMAN_PHONE` 同一状态把 `pose` 写为“右手竖持自拍”，却把 `supports` 与 `contacts` 写为 `BAG.interior`。详见独立 `beach-semantic-review.json`；这是人工跨字段发现，脚本没有自动理解该矛盾，须由上游修订 AVIR 后重编译。正文删除离画手机重复状态不等于解决来源冲突。

本轮只有静态编译、字节覆盖、导入与包结构验收；实际模型生成质量、人物连续性和声画效果需要另行受控 A/B 与媒体审阅。

## 2026-09-24 · 1.5.0 按模型时长交付独立文件

- 技能格式校验通过；完整技能回归 100 项通过，新增逐段交付回归 3 项复测通过。逐段文件的真实图片名、正文覆盖、文件篡改检测和后期声音交接均有检查。
- 海边街拍 60 秒 AVIR 分别编译 Seedance 2.0 与 2.5，并通过包级 `verify`。2.0 输出 0–15、15–30、30–45、45–60 秒四份正文；后两份因 45 秒缺关键姿态且穿过动作/运镜/运动轨，保留全文但标 `BLOCKED`。2.5 输出 0–30、30–60 秒两份独立正文，逐段状态为 `DRAFT_REQUIRES_TARGET_CHECK`。每份都有独立覆盖文件，相关后期声音另附交接文本；全片 60 秒 `prompt.txt` 仍因单次时长超限保持 `BLOCKED`。
- 以上仅证明按已记录模型时长分段、文本与本地参考映射保真及包完整性；网页/账户附件槽位、提示词长度上限、实际生成质量与跨段拼接效果未核验，没有提交视频。
