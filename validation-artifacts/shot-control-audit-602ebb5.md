# 审核结论：Approve——限定为本次失败实验记录与 PNG 追记

已通过 GitHub 将短 SHA 解析并固定为：

```text
目标：602ebb5fd378ed12545f94a44ebf0fe44c81b208
父提交：0b281c593841211fd3727a8d815dd9008b0283cb
标题：Archive composition guide audit and PNG provenance
```

父提交的上一提交确为 `1f27afe6e4693db5607c58959894af6677eef2e2`，与本轮要求的增量链一致。

**没有发现阻断实验记录归档的 P1/P2 问题。** 发现一项 **P3 文字顺序错误**：实施记录把按“后沿、前沿”排列的误差写成了“前后沿”。结构化观测、r3 README 和算术结果正确，整体 FAIL 不受影响。

:chatgpt-content-reference{index="15"}[下载完整原始报告](sandbox:/mnt/data/audit-602ebb5/AUDIT-REPORT.zh-CN.md)　｜　:chatgpt-content-reference{index="16"}[下载可复跑用例与证据包](sandbox:/mnt/data/602ebb5-experiment-record-audit.zip)　｜　:chatgpt-content-reference{index="17"}[查看机器可读摘要](sandbox:/mnt/data/audit-602ebb5/evidence/summary.json)

文件已确认存在；ZIP 完整性及包内逐文件 SHA-256 校验通过。证据包**不含私人摄影输出或身份照片**。

## 一、本轮实际验证了什么

容器直连 GitHub 返回 DNS 解析错误，后续通过 GitHub 连接器取文。新文件依据连接器内容落盘；未变的 r2 文件从上轮证据包复用，并重新核对当前提交所报的 Git blob。

本轮完成 **11 个完整公开文件的 Git blob 复算**，包括 r3 四文件、r2 六文件及审核归档。实施记录则核对了 GitHub blob 元数据、两次提交的 diff 和目标末段，**未下载完整实施记录，不计入这 11 个全文件复算**。

| 增量文件 | 目标 Git blob |
|---|---|
| r3 `README.md` | `97ce8b2b4aff5d03d240f2af0f5995018be8f3cf` |
| r3 `frozen-experiment.json` | `c196580ae0b5e2e52897ff432d82a33208c9035e` |
| r3 `observation.json` | `ede98afa27d6651d46295140cf711e3be14bfad5` |
| r3 `prompt.txt` | `8ebd6ff31db19cc4a6d5ba1343bae15e8c320d64` |
| r2 `README.md` | `6da9816798f51dbfe387c2f857cc0557ec3e18bc` |
| 上轮审核归档 | `ec71bbc9017617721dfa8b84f0737ed5d55091ca` |
| 实施记录，仅元数据与变更内容核对 | `1defd9fd959d2e4de9a5ffc49b8334274d354f8c` |

完整审计脚本实际执行并返回退出码 **0**：**56 项公开文件、字段、算术和像素检查符合预期**，另列 P3 及证据边界提示。该数量不是 Skill 回归测试，更不是生成质量通过数。

七个 Skill 目录与 `dists` 的 tree SHA，在基线、父提交和目标提交中一致；本轮没有重跑或重新计入旧软件审核通过数。

## 二、r3 的输入与验收合同一致

### 输入变化有明确记录

冻结列表与 prompt 对应的顺序为：

| 顺序 | 输入 | 职责 |
|---|---|---|
| 1 | r2 摄影结果 | 编辑基图，保留人物、服装、场景和光线 |
| 2 | r2 扁平 guide | 仅提供摄影机画面中的桌沿和信封位置 |
| 3 | A 身份母版 | 左侧人物身份 |
| 4 | B 身份母版 | 右侧人物身份 |

r2 空景母版的路径和摘要均未出现在 r3 输入列表中。第一项摘要与 r2 观测记录的输出摘要一致；第二项与公开 guide 的实际字节一致；最后两项与前轮身份摘要声明一致。**后三类核对中的私人图片部分只是跨记录对应，不是实际图片字节验证。**

实际计算的 r3 prompt SHA-256 为：

```text
95b2c5be9a2e7a8cd8fc939a0abdd70187960c641ff996d025e4744a42f80201
```

与冻结表相符。prompt 的图片编号、编辑模式、桌沿位置及信封百分比，也与冻结记录对应；没有把 guide 表述成俯视运动轨迹。

### 桌沿阈值这次单独定义了

冻结表明确保存：

```text
信封中心：(0.37836213115936546, 0.7162451001611281)
信封每轴容差：0.02

桌面后沿：0.775，容差 0.02
桌面前沿：0.897，容差 0.02
```

这些值与 observation 一致。与 r2 不同，**r3 的两条桌沿都有各自的数值验收门槛**，不需要事后借用信封容差。

但“记录写有调用前冻结”与“独立证实调用前冻结”仍是两件事。本轮不能认证实际保存先后、输入顺序或调用次数。

## 三、算术正确，给定不确定性后 FAIL 仍成立

**以下以作者记录的尺寸和手工标注为条件，不代表我确认了摄影图中的位置。** 使用 `Decimal`、按 `x/W、y/H` 复算：

| 测量项 | 归一化值 | 对目标误差 | 纳入标注不确定性的误差区间 | 条件判断 |
|---|---:|---:|---|---|
| 信封 x：759±12 px | 0.453947368421 | +0.075585237262 | **[+0.068408204, +0.082762271]** | FAIL |
| 信封 y：662±10 px | 0.703506907545 | −0.012738192616 | **[−0.023365185, −0.002111200]** | UNDETERMINED |
| 后沿 y：778±8 px | 0.826780021254 | +0.051780021254 | **[+0.043278427, +0.060281615]** | FAIL |
| 前沿 y：906±10 px | 0.962805526036 | +0.065805526036 | **[+0.055178533, +0.076432519]** | FAIL |

observation 和 README 的六位小数舍入正确。

信封 x 与两条桌沿，即使采用区间中最有利于合格的端点，仍超出 `+0.02`，所以记录中的 **`FAIL_X`、`FAIL_BOTH_EDGES`、总 `FAIL`** 有条件算术支持。

信封 y 的点估计落在阈值内，但区间跨过 `−0.02`，不能另行宣布该轴 PASS。现有记录没有这样做。这里将 ±像素作为有界敏感性范围使用，**未赋予统计置信度，也未扩大验收阈值**。

## 四、D01／P3：实施摘要的前后沿顺序写反

**位置：**精确目标提交的 `IMPLEMENTATION-SHOT-CONTROL.md:266`。该句在 `0b281c5` 引入，在 `602ebb5` 保留：

> 桌面前后沿误差 +0.051780/+0.065806

按“前后沿”的文字顺序读，会把较小误差归给前沿；实际应是：

```text
后沿：778/941 − 0.775 = +0.051780021254
前沿：906/941 − 0.897 = +0.065805526036
```

r3 README 的 *rear and front* 与 JSON 顺序正确，错误只在实施摘要的标签。

**最小修复：**

```diff
- 桌面前后沿误差 +0.051780/+0.065806
+ 桌面后沿误差 +0.051780，前沿误差 +0.065806
```

**验收门：**摘要边名与结构化字段一一对应，原冻结表、测量值和 FAIL 不变。无需重生成图片，属于非阻断 P3。

## 五、r2 PNG 追记没有改写历史输入

本轮确认 r2 的 frozen、绘图脚本、guide、observation 和 prompt 字节均未改变，README 是追加说明。

公开 guide 的实际核对结果：

```text
模式／尺寸：RGB，1672×941
PNG SHA-256：
0efd1a2c1cf8819bdee186d18b9b1d5df9680ed11803cfd20b120de1ade41182

RGB 原始像素 SHA-256：
be7f37196d9196549a0328530f660acec7602ee02ccdb29ec4f194977e308004
```

本轮在 **Python 3.13.5／Pillow 12.3.0** 下又原样重画两次：结果均为 11,690 字节，PNG 摘要为 `b053d528…069399`；与公开原图的 **RGB 像素及解压过滤扫描行完全相同**。原 guide 和 frozen 未被覆盖。

README 的“本机 Python 3.12.13／Pillow 12.2.0／zlib 1.2.12”明确放在**复审后追记**中。本轮没有运行该环境，也没有证明它是历史原执行环境；应将其视为作者的事后本机复现报告。追记正确区分了文件字节身份和跨环境像素复现，没有把压缩字节差异判成构图漂移。

### 原审核归档的边界

归档正文与本会话上一轮最终回复逐段对照，未见新增实测、质量改判或删除核心限制。**它保存的是上一轮最终回复的转存文本，不是下载版技术报告的逐字节副本。**

转存中去掉了交互式引用，并残留三个 `:chatgpt-content-reference` 标记和会话 `sandbox:` 链接。它们是格式／可移植性问题，不构成结论篡改；也不能把这些会话链接当作 GitHub 上可直接访问的附件。

## 六、未验证项目与最终交付边界

本轮没有取得 r3 摄影输出 `378e508e…040b2d`、r2 摄影基图或身份母版。因此，以下项目仍**未独立验证**：私人文件哈希、真实尺寸、手工标注及不确定范围、视觉质量、身份、手别、材质，以及工具调用次数、冻结先后和供应商元数据。

记录保留了 `handedness=UNDETERMINED`、供应商模型／编号／费用为 `null`、未走产品 `keyframe_host`、视频 `NOT_RUN`；没有把未知费用写成零费用，也没有将实验图认作合格首帧。

复跑本轮公开记录检查：

```bash
python inspect_records.py --out /absolute/path/to/new-results
```

该完整入口本轮已运行。没有运行旧 Skill 回归、产品宿主链、Blender、VACE 或商业视频接口。

**最终意见：Approve 本次限定范围的记录提交，建议修正 D01／P3。PR3 保留构图 FAIL；PR4 的真实商业首尾帧入口、预算、上传、请求和实收仍未完成。本次批准不等于私人图片质量通过或完整方案交付。**
