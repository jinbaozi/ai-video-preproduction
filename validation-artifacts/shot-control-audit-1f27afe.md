## 结论：Approve——限定为本次实验记录

本轮实际查询并固定到：

```text
提交：1f27afe6e4693db5607c58959894af6677eef2e2
父提交：5a81d0c3a6362b5321fb3c9f5588f6932b9ee4cf
标题：Record controlled composition-guide keyframe failure
```

与指定目标一致。七个 Skill 目录及 `dists` 的 Git tree SHA 与父提交相同，**本次没有修改这些源码或发行包，也没有重新计入旧审核通过数**。

**没有发现阻断该失败实验归档的 P1/P2 问题。** 发现一项 P3 可复现性提示：原样重画的 PNG **文件字节摘要不同，但解码像素完全一致**。其余重点包括坐标计算、阈值、不确定性和未执行状态，没有发现被改写成生成成功。

:chatgpt-content-reference{index="15"}[下载完整原始报告](sandbox:/mnt/data/audit-1f27afe/AUDIT-REPORT.zh-CN.md)　｜　:chatgpt-content-reference{index="16"}[下载可复跑脚本、公开素材与证据包](sandbox:/mnt/data/1f27afe-experiment-record-audit.zip)　｜　:chatgpt-content-reference{index="17"}[查看结果摘要](sandbox:/mnt/data/audit-1f27afe/evidence/summary.json)

以上文件已确认存在；证据 ZIP 通过完整性和逐文件 SHA-256 校验，**不含未公开的摄影 PNG 或身份照片**。

## 一、实际验证范围

本轮取得并核对了实验目录全部六份公开文件的 Git blob：

| 文件 | Git blob |
|---|---|
| `draw_guide.py` | `611b9089aa26be8ee20938ee07b09be15d7192cd` |
| `guide.png` | `8fd3ce99f9ef68249c4cb850dd1eed68d3d1e16a` |
| `frozen-experiment.json` | `2c719eca42371b447657b75e1b8dc02bb19f70dc` |
| `prompt.txt` | `96ac0ec4def12d42f9ba13594455de264865d9f3` |
| `observation.json` | `b2ccee7624e3536ffaffe084a1f9c450bb3d34c4` |
| `README.md` | `d52f244bdd25077f19ee2bbe2a04b97997bbdb1b` |

这些值与精确提交目录中的记录一致。

实际执行了两次原样绘图、公开图与重画图的像素／PNG 数据比较，以及使用 `Decimal` 的坐标与误差复算。审计脚本返回 **34 项记录检查通过，另单列 PNG 字节不一致警告**。这些是文件和算术检查，不是 Skill 回归或生成质量测试。

还实际查看了**公开扁平 guide**：它是正面摄影机画面中的人物轮廓、桌面和信封排布；不是摄影输出，也不是俯视调度图。

## 二、N01／P3：重画内容一致，冻结文件字节未复现

**位置：**`draw_guide.py:40–42`，重点为：

```python
im.save(out, optimize=True)
```

脚本没有记录编码环境。其自身 Git blob 和冻结脚本摘要都匹配，公开原图也匹配冻结摘要。

### 有效前置与实际结果

在 Python 3.13.5、Pillow 12.3.0 环境中，将**未经修改的原脚本**复制到两个新目录分别执行：

| 项目 | 公开冻结图 | 本地原样重画 |
|---|---:|---:|
| 模式／尺寸 | RGB，1672×941 | RGB，1672×941 |
| 文件大小 | 9,981 字节 | 11,690 字节 |
| SHA-256 | `0efd1a2c…de41182` | `b053d528…069399` |
| IDAT 压缩数据大小 | 9,924 字节 | 11,633 字节 |
| RGB 像素差 | — | **0 像素** |
| 解压后的过滤扫描行 | — | **完全相同** |

两次本地重画的文件字节也相同。两图共同的 RGB 字节摘要为：

```text
be7f37196d9196549a0328530f660acec7602ee02ccdb29ec4f194977e308004
```

**因此，不能写“本轮重画恢复了冻结 PNG 的文件摘要”，也不能反过来把它判成构图变化。** 已定位的区别在 PNG 压缩表示，不在绘图内容；没有作者完整编码环境，不能进一步断言其具体版本差异。

### 最小完善建议

保留公开原 PNG 和冻结文件摘要，**不要用重画摘要回填历史输入**。补记生产端 Pillow／压缩库版本，并增加模式、尺寸及 RGB 内容摘要，区分“输入文件身份”与“重画内容等价”。

验收门是：改像素必须被识别；相同像素的不同编码不能误报为构图漂移；需要逐字节复现时，在锁定的支持环境中验证。该提示不要求重新生成摄影图，也不阻断本次失败记录归档。

## 三、坐标计算正确；信封两轴都受临界不确定性影响

**以下复算以作者记录的尺寸和标注为输入，不表示我独立确认了摄影图尺寸或标注位置。**

| 项目 | 本轮复算 | 记录值 |
|---|---:|---:|
| 信封 x | `674/1672 = 0.403110047847` | `0.403110` |
| 信封 y | `657/941 = 0.698193411265` | `0.698193` |
| 信封 Δx | `+0.024747916688` | `+0.024748` |
| 信封 Δy | `−0.018051688897` | `−0.018052` |
| 桌面后沿 y | `810/941 = 0.860786397450` | `0.860786` |
| 桌面后沿 Δy | **`+0.085786397450`** | 未单列差值 |

记录中的舍入均正确。信封目标和每轴 ±0.02 在冻结表与观察表中一致，没有因结果不佳而放宽。

将 ±12 像素标注不确定性暂按有界区间做敏感性计算，得到：

```text
Δx ∈ [+0.017570883195, +0.031924950180]
Δy ∈ [−0.030804079970, −0.005299297823]
```

**两轴都跨越各自容差边界。** x 的点估计超出 +0.02；y 的点估计未超界，但其不确定范围跨过 −0.02。因此，信封单项保留 `UNDETERMINED` 而不是 PASS 是合适的。`UNDETERMINED_NEAR_X_THRESHOLD` 可补充 y 轴的说明，但当前总状态没有误报通过。这里没有把 ±12 像素当成统计置信区间，更没有加到验收阈值上。

桌沿 `810±10` 像素对应：

```text
Δy ∈ [+0.075159404888, +0.096413390011]
点估计相差 +80.725 像素
```

数值方向与“桌沿低于目标”一致。**但冻结表只给桌沿目标，没有桌沿专属数值容差，prompt 使用的是 “around”。** 所以记录中的桌沿 FAIL 应视为作者的定性构图判断，不应宣称已按完整数值规则独立复验。后续实验可事前补齐桌沿容差，不能事后套用信封的 ±0.02。

## 四、输入职责和证据边界总体清楚，仍有三点需区分

### 1. 输入顺序一致，不等于实际调用顺序已获认证

冻结列表和 prompt 均为：

```text
Image 1：guide
Image 2：A 身份／服装
Image 3：B 身份／服装
Image 4：咖啡馆材质／光线
```

prompt 明确禁止场景母版的桌面位置和机位取代 guide 的布局；目标百分比与冻结坐标的舍入一致，桌沿 0.775／0.897 也一致。

有一处非阻断的描述差别：`frozen-experiment.json:13` 写 **composition only**，`prompt.txt:3` 写 **composition and pose authority**。下一次冻结时建议明确 guide 也提供粗略姿态／伸手位置参考，避免职责文字不一致；它仍不应承担身份、真实手别或解剖验证。

### 2. 文件一致，不能独立证明“事前冻结”

可以确认脚本、prompt、公开 guide 与冻结摘要之间的关系。但 `FROZEN_BEFORE_SUBMISSION` 是作者记录，当前目录也明确没有产品 `stage/receipt`。**公开提交本身不足以认证实际调用前后的时间顺序、附件顺序和一次调用次数。**

这属于证据边界，不应要求补造供应商 ID、stage 或时间戳。后续实验应保存当时的冻结摘要与脱敏调用关联。

### 3. 没有把参考图当成轨迹控制或合格首帧证明

README 明确区分了相机画面排布与俯视轨迹图，也说明未经过产品宿主链、不作为合格可消费首帧、没有视频请求。观察表保留：

```text
overall_result          = FAIL
envelope_position_result = UNDETERMINED_NEAR_X_THRESHOLD
handedness_observation   = UNDETERMINED
host_model / host_execution_id / host_price = null
keyframe_host_stage     = NOT_CREATED_FOR_THIS_EXPERIMENT
video_execution         = NOT_RUN
```

没有把未知费用写成零费用，也没有把 guide 哈希当成模型服从度。前次与本次点误差的数值比较成立，但 README 同时承认方法与参考条件一起变化，不能证明 guide 的独立因果收益。

## 五、未独立验证与最终交付边界

本轮**未取得**摄影输出 `f326481d…d24b03` 的字节或像素。因此，其文件哈希、尺寸、手工标注、标注不确定性、视觉质量、身份、手别、材质和额外物件均未独立验证；三张摄影母版及供应商执行元数据也未验证。报告没有提供该私人输出的下载路径。

本轮没有运行旧 Skill 回归、产品宿主链、Blender、VACE 或商业视频接口。**PR3 仍保留作者记录的构图 FAIL；PR4 的真实首尾帧入口、预算、上传、请求和实收仍未完成。** 实施记录没有把本次失败实验升级为这些环节的通过证据。

复跑本轮公开文件检查：

```bash
python inspect_experiment.py --out /absolute/path/to/new-results
```

该完整入口本轮已实际执行。**本次批准的是边界清楚、可核对的失败实验记录，不是摄影图质量 PASS、可消费关键帧或完整方案交付。**
